---
mountpoint: "/go/src/github.com/tinyci/ci-runners"
env:
  - GO111MODULE=on
runs:
  checks:
    command: [ "bash", "checks.sh" ]
    image: "golang:1.13"
  build:
    command: [ "make" ]
    image: "golang:1.13"
  # staticcheck:
  #   command: [ "make", "staticcheck" ]
  #   image: "golang:1.12.6"
